"""NARA desktop shell (Phase 5) — a lightweight macOS menu-bar app.

Uses rumps (pure Python) rather than Tauri/Electron so there's no Rust/Node
build. A native SwiftUI/Tauri HUD is a future upgrade on top of core/service.py.
"""
