"""Vault skill — expose read/search/write of the Obsidian vault as a tool.

Thin wrapper over ``core.memory.MemoryManager`` so the orchestrator can call
the vault as a registered tool. Implemented alongside Phase 1/Phase 2.
"""
